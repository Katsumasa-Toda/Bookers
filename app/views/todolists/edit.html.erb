<h1>編集画面</h1>
<%= form_with model:@book, url:"/todolists/#{@book.id}", local:true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :本のタイトル %>
  <h4>本文</h4>
  <%= f.text_area :感想 %>
  <%= f.submit '保存' %>
<% end %>